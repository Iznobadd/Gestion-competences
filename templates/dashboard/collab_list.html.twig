{% extends 'base.html.twig' %}

{% block title %}Add Skill{% endblock %}

{% block body %}
    {% set i = 1 %}
<h1>Collab list</h1>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">First</th>
            <th scope="col">Last</th>
            <th scope="col">Status</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
        <tr>
            <th scope="row"> {{ i }}</th>
            <td>{{ user.firstName }}</td>
            <td>{{ user.lastName }}</td>
            <td>{% if user.status %}Available{% else %}Not available{% endif %}</td>
        </tr>
            {% set i = i+1 %}
        {% endfor %}
        </tbody>
    </table>
{% endblock %}